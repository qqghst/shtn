import React from 'react';
import styles from './styles.module.scss';

const Navbar: React.FC = () => {
	return (
		<nav className={styles.navbar}>
			<div className={styles.navbar__container}>1</div>
		</nav>
	);
};

export default Navbar;
