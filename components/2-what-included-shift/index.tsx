import React from 'react';
import styles from './styles.module.scss';

const WhatIncludedShift: React.FC = () => {
	return (
		<section className={styles.whatIncludedShift}>
			<div className={styles.whatIncludedShift__container}></div>
		</section>
	);
};

export default WhatIncludedShift;
